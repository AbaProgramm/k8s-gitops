apiVersion: v1
kind: Service

metadata:
  name: nginx
  namespace: training

spec:
  type: LoadBalancer

  selector:
    app: nginx

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
